# Icons and UI Intake

Drop source artwork for inventory icons, action icons, interaction prompts, map markers, menu icons, HUD pieces, buttons, panels, UI frames and status symbols here.

Files placed here are staging files. After approval, Artifex should copy them into the final project `assets/` folders and register their final paths in the Asset Library.
