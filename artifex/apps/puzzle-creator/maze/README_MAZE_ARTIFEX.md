# Maze module

This folder is the recovered Artifex Puzzle Creator shell configured to open directly into the Labyrinth Maze workflow.

It preserves the original Artifex-style header, left control rail, main maze preview, and floating Overview panel shown in the reference screenshot.

Open `index.html` in this folder directly, or use the top-level launcher menu.
