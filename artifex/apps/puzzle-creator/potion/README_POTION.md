# Potion Match module

This folder contains the standalone Potion Match V2 Dual Mode page as `index.html`.

Also included:
- `src/js/engines/potion-match-runtime.js`
- `POTION_MATCH_INSTALL.txt`

The standalone page is the one to open directly. The runtime file is preserved for later reintegration into Artifex if needed.
