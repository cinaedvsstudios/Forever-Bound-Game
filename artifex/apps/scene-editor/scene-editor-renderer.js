(() => {
  'use strict';

  function esc(value) {
    return String(value ?? '').replace(/[&<>"']/g, match => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[match]));
  }

  function createRenderer(deps) {
    function assetPath(path) {
      if (!path) return '';
      if (/^(https?:|data:|blob:|\/|\.\.\/)/i.test(path)) return path;
      return deps.repoPrefix + path.replace(/^\.\//, '');
    }

    function filePill() {
      const state = deps.getState();
      const working = deps.readWorkingCopy();
      const downloaded = deps.readDownloadStamp();
      const activeProject = deps.getActiveProject?.();
      const projectText = activeProject?.projectName || activeProject?.projectId || 'No active project';
      const projectClass = activeProject ? '' : ' file-pill-no-project';
      const chooseAction = activeProject ? '' : `<a class="file-pill-project-action" href="../creation-guide/?fresh=active-project-client&amp;from=${encodeURIComponent(location.pathname)}" title="Choose an active project in the Hub">Choose in Hub</a>`;
      return `<div class="file-pill${projectClass}"><span class="file-pill-line file-pill-project"><span class="file-pill-icon">🏗️</span><span class="file-pill-label">Project:</span> <span class="file-pill-value">${esc(projectText)}</span>${chooseAction}</span><span class="file-pill-line file-pill-file"><span class="file-pill-icon">📁</span><span class="file-pill-label">File:</span> <span class="file-pill-value file-pill-name">${esc(state.fileName || 'Untitled JSON')}</span></span><span class="file-pill-line file-pill-save"><span class="file-pill-icon">💾</span><span class="file-pill-label">Local:</span> <span class="file-pill-value">${esc(deps.dateText(working?.savedAt))}</span> <span class="file-pill-sep">|</span> <span class="file-pill-label">HDD:</span> <span class="file-pill-value">${esc(deps.dateText(downloaded?.downloadedAt))}</span></span></div>`;
    }

    function resumeMarkup() {
      const working = deps.readWorkingCopy();
      if (!working?.scene) return `<span>Import a JSON from hard drive, URL, or templates to begin.</span><div class="artifex-version-marker">${deps.version} inspector-controls repair build</div>`;
      const downloaded = deps.readDownloadStamp();
      return `<div class="resume-card-inline"><h3>Start where you left off?</h3><p>You were last working on:</p><strong>${esc(working.fileName || working.scene.id || 'Untitled JSON')}</strong><p>Last local backup: ${esc(deps.dateText(working.savedAt))}</p><p>Last downloaded: ${esc(deps.dateText(downloaded?.downloadedAt))}</p><div class="resume-actions"><button class="btn" id="openLocalBackup" type="button">Open local backup</button><button class="btn" id="ignoreLocalBackup" type="button">Ignore</button></div></div>`;
    }

    function menuItem(label, action, disabled = false) { return `<button type="button" class="editor-menu-item-v25" data-menu-action-v25="${action}" ${disabled ? 'disabled' : ''}>${label}</button>`; }
    function menuBlock(label, action, body) { return `<div class="editor-menu-v25" data-menu-v25="${action}"><button type="button" class="editor-menu-button-v25" data-open-menu-v25="${action}">${label}</button><div class="editor-menu-dropdown-v25">${body}</div></div>`; }
    function menuBar() { return `<nav class="editor-menubar-v25" aria-label="Scene editor menu">${menuBlock('File', 'file', `${menuItem('New Blank Scene', 'newBlank')}${menuItem('New from Template', 'newTemplate')}${menuItem('Download JSON', 'download')}${menuItem('Settings', 'settings')}`)}${menuBlock('Edit', 'edit', `${menuItem('Undo', 'placeholder', true)}${menuItem('Redo', 'placeholder', true)}${menuItem('Duplicate Selected', 'duplicate')}`)}${menuBlock('View', 'view', `${menuItem('Toggle Guides', 'toggleGuides')}${menuItem('Reset Zoom', 'resetZoom')}${menuItem('Preview Selected', 'preview')}`)}${menuBlock('Effects', 'effects', `${menuItem('Visual Adjustments', 'placeholder', true)}${menuItem('Glow Colour', 'placeholder', true)}`)}${menuBlock('Help', 'help', `${menuItem('Help', 'help')}${menuItem('Shortcuts', 'placeholder', true)}`)}</nav>`; }
    function titleBar() { const state = deps.getState(); return `<header class="top-bar has-menubar-v25"><div class="brand" data-tip="Scene Editor · ${deps.version}"><img class="brand-logo" src="${deps.brandLogo}" alt="Artifex logo"><span class="brand-title-stack" style="display:inline-flex;flex-direction:column;align-items:flex-start;gap:2px;line-height:1;"><img class="brand-title" src="${deps.brandTitle}" alt="Artifex"><span class="brand-subtitle" style="color:#f1dcc2;font-size:10px;letter-spacing:.08em;text-transform:uppercase;text-shadow:0 0 8px rgba(195,0,255,.45);white-space:nowrap;">Scene Editor · ${deps.version}</span></span></div>${menuBar()}<button class="btn manual-local-save" id="manualLocalSave" type="button" data-tip="Force save to local browser backup.">💾</button><span class="title-divider"></span><div class="import-menu ${state.importOpen ? 'is-open' : ''}" id="importMenu"><button class="import-button" id="importBtn" type="button" data-tip="Import JSON from hard drive, URL, or template.">Import ▾</button><div class="import-dropdown"><label class="file-button">From hard drive<input class="hidden-file" id="jsonFile" type="file" accept=".json,application/json"></label><button class="btn" id="importUrl" type="button">From URL</button><button class="btn" id="importTemplate" type="button">From templates</button></div></div><button class="btn" id="downloadJson" type="button" data-tip="Download the current JSON.">Download JSON</button><button class="btn" id="blankBtn" type="button" data-tip="Clear to blank editor.">Blank Screen</button><span class="tooltip-status" id="hoverStatus">${esc(state.tip)}</span><span class="status ok">${esc(state.status)}</span><span class="top-spacer"></span><a class="btn" href="../../">Portal</a></header>`; }

    function card(id, title, body, tone = '') {
      const isCollapsed = deps.getState().collapsed[id];
      return `<section class="panel-card ${tone ? `card-${tone}` : ''} ${isCollapsed ? 'is-collapsed' : ''}" data-card-id="${id}"><h2><span>${esc(title)}</span><button class="card-toggle" type="button" data-card-toggle="${id}">↕</button></h2><div class="card-body">${isCollapsed ? '' : body}</div></section>`;
    }

    function input(label, id, value, type = 'text') { return `<div class="field"><label for="${id}">${label}</label><input id="${id}" type="${type}" value="${esc(value)}"></div>`; }
    function checkbox(label, id, checked) { return `<label class="check-row"><input id="${id}" type="checkbox" ${checked ? 'checked' : ''}> ${esc(label)}</label>`; }
    function settingsSearch() { return `<div class="settings-search" role="search"><label class="settings-search-label" for="settingsSearch">Search Settings</label><input id="settingsSearch" type="search" placeholder="Search settings…" title="Search settings by card title or visible control label" aria-label="Search settings by card title or visible control label" autocomplete="off"><p class="settings-search-empty" id="settingsSearchEmpty" hidden>No matching settings</p></div>`; }

    function typeSelect(value) {
      const normalized = String(value || 'prop');
      const options = deps.typeOptions.includes(normalized) ? deps.typeOptions : [normalized, ...deps.typeOptions];
      return `<div class="field"><label for="itemType">Type</label><select id="itemType">${options.map(type => `<option value="${esc(type)}" ${type === normalized ? 'selected' : ''}>${esc(type)}</option>`).join('')}</select></div>`;
    }

    function pathInput(label, id, value, target) { return `<div class="field path-field"><label for="${id}">${label}</label><div class="path-row"><input id="${id}" type="text" value="${esc(value)}"><div class="path-menu"><button class="path-menu-toggle" data-path-menu="${target}" type="button">📁</button><div class="path-dropdown"><button type="button" data-online="${target}">Online</button><button type="button" data-hdd="${target}">HDD</button><button type="button" data-assets="${target}">Assets</button></div></div></div></div>`; }
    function basics() { const scene = deps.getState().scene; return `${input('Scene ID', 'sceneId', scene.id)}${input('Scene Name', 'sceneName', scene.name)}${input('Screen Type', 'sceneType', scene.screenType || scene.mode)}${input('Scene Tags', 'sceneTags', Array.isArray(scene.tags) ? scene.tags.join(', ') : scene.tags || '')}<div class="field-row">${input('Grid Columns', 'gridCols', scene.grid.columns, 'number')}${input('Grid Rows', 'gridRows', scene.grid.rows, 'number')}</div><label class="check-row"><input id="gridShow" type="checkbox" ${scene.grid.show !== false ? 'checked' : ''}> Show grid</label>`; }
    function background() { return `${pathInput('Background Image Path', 'sceneBg', deps.backgroundPath(), 'background')}`; }
    function sceneAudio() { const audio = deps.getState().scene.audio || {}; return `${pathInput('Ambience Audio Path', 'sceneAudioAmbience', audio.ambience || audio.ambient || '', 'sceneAudioAmbience')}${pathInput('Music Track Path', 'sceneAudioMusic', audio.music || audio.track || '', 'sceneAudioMusic')}<div class="field-row">${input('Scene Volume', 'sceneAudioVolume', audio.volume ?? 100, 'number')}${input('Fade In Seconds', 'sceneAudioFadeIn', audio.fadeIn ?? 0, 'number')}${input('Fade Out Seconds', 'sceneAudioFadeOut', audio.fadeOut ?? 0, 'number')}</div>${checkbox('Loop scene audio', 'sceneAudioLoop', audio.loop !== false)}`; }
    function elements() { const state = deps.getState(); const item = deps.getSelectedItem(); const locks = (() => { try { return JSON.parse(localStorage.getItem('artifex.sceneEditor.layerLocks.v1') || '{}'); } catch { return {}; } })(); return `<div class="button-row compact-actions layer-control-row"><label class="layer-pill">Layer <input id="layerPill" type="number" value="${esc(item?.layer ?? item?.z ?? 0)}"></label><button class="btn clean-layers-btn recalculate-layers-btn" id="recalculateLayersBtn" type="button">🔁 Recalculate</button></div><div class="item-list layer-stack-table-v14">${deps.getAllItems().sort((a, b) => Number(b.layer || 0) - Number(a.layer || 0)).map((it, idx) => `<div class="layer-stack-row" data-slot="${idx + 1}" draggable="true"><span class="layer-slot-number">${idx + 1}</span><button class="btn item-row layer-stack-pill ${it.id === state.selectedId ? 'is-selected' : ''}" data-select-kind="${it.kind}" data-select-id="${esc(it.id)}" type="button">${esc(it.name || it.id)} · ${esc(it.type || it.kind)}</button><button type="button" class="layer-lock-btn" title="${locks[it.id] ? 'Unlock layer slot' : 'Lock layer slot'}">${locks[it.id] ? '🔒' : '🔓'}</button></div>`).join('') || '<p class="small">No elements.</p>'}</div>`; }
    function sizeShapeControls(item) { return `<div class="size-shape-controls-v35"><h3>Size / Shape</h3><div class="button-row compact-actions"><button class="btn" type="button" data-transform-action="scale-down">Scale −</button><button class="btn" type="button" data-transform-action="scale-up">Scale +</button><button class="btn" type="button" data-transform-action="wrap-image">Wrap Bounding Box to Image</button><button class="btn aspect-lock-btn-v35 ${item.aspectRatioLocked === true ? 'is-active' : ''}" type="button" data-transform-action="toggle-aspect" aria-pressed="${item.aspectRatioLocked === true ? 'true' : 'false'}">Aspect Ratio Lock</button><input id="itemAspectLock" type="checkbox" ${item.aspectRatioLocked === true ? 'checked' : ''} hidden readonly></div></div>`; }
    function selectedForm(item) { return `${input('ID', 'itemId', item.id)}${input('Name', 'itemName', item.name || item.label || '')}${typeSelect(item.type || '')}${pathInput('Image Path', 'itemImage', item.image || '', 'item')}${input('Text', 'itemText', item.text || '')}`; }
    function transformForm(item) { return `<div class="field-row"><div class="field"><label for="itemRotation">Rotate</label><input id="itemRotation" type="number" min="-180" max="180" step="1" value="${esc(item.rotation ?? 0)}"></div><div class="field"><label for="itemRotationOrigin">Rotation Origin</label><select id="itemRotationOrigin">${['topLeft','top','topRight','left','centre','right','bottomLeft','bottom','bottomRight'].map((origin) => `<option value="${origin}" ${origin === (item.rotationOrigin || 'centre') ? 'selected' : ''}>${origin}</option>`).join('')}</select></div></div><div class="field-row">${input('X Axis', 'itemX', item.x ?? 10, 'number')}${input('Y Axis', 'itemY', item.y ?? 10, 'number')}</div><div class="field-row">${input('Width', 'itemW', item.width ?? 10, 'number')}${input('Height', 'itemH', item.height ?? 10, 'number')}</div><div class="field-row">${input('Layer', 'itemLayer', item.layer ?? item.z ?? 10, 'number')}<div class="field"><label>Z / Depth <span class="range-value" id="zVal">${esc(item.zDepth ?? 0)}</span></label><input id="itemZ" type="number" min="-20" max="20" step="1" value="${esc(item.zDepth ?? 0)}"></div></div><div class="field-row skew-controls-v19"><div class="field"><label for="itemSkewX">Skew X</label><input id="itemSkewX" type="number" step="1" value="${esc(item.skewX ?? 0)}"></div><div class="field"><label for="itemSkewY">Skew Y</label><input id="itemSkewY" type="number" step="1" value="${esc(item.skewY ?? 0)}"></div></div><label class="check-row"><input id="itemVisible" type="checkbox" ${item.visible !== false ? 'checked' : ''}> Visible</label><div class="button-row"><button class="btn" id="deleteItem" type="button">Delete Selected</button></div>${sizeShapeControls(item)}`; }
    function visualAdjustmentsForm(item) { const visual = { blendMode: 'normal', opacity: 100, brightness: 100, contrast: 100, saturation: 100, hue: 0, vibrance: 0, exposure: 0, shadowStrength: 0, shadowBlur: 25, glowStrength: 0, ...(item.visual || {}) }; const modes = ['normal','screen','multiply','overlay','lighter','darken','lighten','color-dodge','color-burn','hard-light','soft-light','luminosity']; const num = (id, label, value, min, max) => `<div class="field visual-live-field-v21"><label for="${id}">${label}</label><input id="${id}" type="number" min="${min}" max="${max}" step="1" value="${esc(value)}"></div>`; return `<div class="v15-card-content visual-card-content-v15 visual-live-controls-v21"><p class="card-layout-note visual-note-v21">Live visual controls. Vibrance is boosted through stronger saturation/contrast because CSS has no native vibrance filter; Exposure is applied as extra brightness.</p><div class="card-layout-group card-layout-2 visual-effects-live-group-v21"><div class="field visual-live-field-v21"><label for="visualBlendModeV21">Blend Mode</label><select id="visualBlendModeV21">${modes.map((mode) => `<option value="${mode}" ${mode === visual.blendMode ? 'selected' : ''}>${mode}</option>`).join('')}</select></div>${num('visualOpacityV21','Opacity',visual.opacity,0,100)}${num('visualBrightnessV21','Brightness',visual.brightness,0,250)}${num('visualContrastV21','Contrast',visual.contrast,0,250)}${num('visualSaturationV21','Saturation',visual.saturation,0,250)}${num('visualHueV21','Hue',visual.hue,-180,180)}${num('visualVibranceV21','Vibrance',visual.vibrance,-100,100)}${num('visualExposureV21','Exposure',visual.exposure,-100,100)}${num('visualShadowStrengthV21','Shadow Strength',visual.shadowStrength,0,100)}${num('visualShadowBlurV21','Shadow Blur',visual.shadowBlur,0,100)}${num('visualGlowStrengthV21','Glow Strength',visual.glowStrength,0,100)}</div></div>`; }

    function controlPanel() {
      const state = deps.getState();
      if (!state.scene) return `<aside class="side-panel"><div class="file-pill">No file loaded</div>${settingsSearch()}${card('blank', 'No Scene Loaded', `<p class="small">Use Import to load a JSON from hard drive, URL, or templates.</p>`, 'basics')}</aside>`;
      return `<aside class="side-panel">${filePill()}${settingsSearch()}${card('basics', 'Scene', basics(), 'basics')}${card('background', 'Background', background(), 'basics')}${card('scene-audio', 'Scene Audio', sceneAudio(), 'basics')}${card('elements', 'Object Layers', elements(), 'elements')}${card('json', 'JSON Preview', `<pre class="json-preview">${esc(JSON.stringify(state.scene, null, 2))}</pre>`, 'json')}</aside>`;
    }

    function letters(i) { let n = i + 1, s = ''; while (n > 0) { const r = (n - 1) % 26; s = String.fromCharCode(65 + r) + s; n = Math.floor((n - 1) / 26); } return s; }
    function gridLabels(cols, rows) { return `<div class="grid-labels">${Array.from({ length: cols }, (_, i) => `<span class="grid-col-label" style="left:${((i + .5) / cols) * 100}%">${i + 1}</span>`).join('')}${Array.from({ length: rows }, (_, i) => `<span class="grid-row-label" style="top:${((i + .5) / rows) * 100}%">${letters(i)}</span>`).join('')}<span class="axis-label axis-x">X</span><span class="axis-label axis-z">Z</span></div>`; }
    function blankMessage() { return `<div class="blank-message"><div><strong>Blank Scene Editor</strong>${resumeMarkup()}</div></div>`; }
    function stageItem(item) { const state = deps.getState(); if (item.visible === false) return ''; const zDepth = Number(item.zDepth || 0), scale = deps.clamp(1 + zDepth * .035, .45, 2.15); const preserve = item.aspectRatioLocked === true ? 'xMidYMid meet' : 'none'; const img = item.image ? `<svg class="scene-image-v33" role="img" aria-label="${esc(item.name || item.id)}" viewBox="0 0 100 100" preserveAspectRatio="${preserve}"><image href="${esc(assetPath(item.image))}" width="100" height="100" preserveAspectRatio="${preserve}"></image></svg>` : `<span class="small">${esc(item.text || item.type || item.id)}</span>`; return `<div class="scene-item ${item.id === state.selectedId ? 'is-selected' : ''}" data-stage-id="${esc(item.id)}" data-stage-kind="${item.kind}" style="left:${item.x ?? 10}%;top:${item.y ?? 10}%;width:${item.width ?? 10}%;height:${item.height ?? 10}%;z-index:${item.layer ?? item.z ?? 1};transform:scale(${scale});">${img}<button class="move-handle" type="button" title="Drag here to move this object" aria-label="Move object"></button><span class="item-label">${esc(item.name || item.id)}</span></div>`; }
    function workArea() { const state = deps.getState(); const background = deps.backgroundPath(); const cols = Number(state.scene?.grid?.columns || 16); const rows = Number(state.scene?.grid?.rows || 9); const gridStyle = `--fine-x:${100 / (cols * 2)}%;--fine-y:${100 / (rows * 2)}%;--major-x:${500 / cols}%;--major-y:${500 / rows}%;`; return `<section class="stage-wrap"><button class="object-preview-btn-v24" type="button" title="Preview selected object cleanly" aria-label="Preview selected object cleanly" ${state.selectedId ? '' : 'disabled'}>👁</button><div class="work-zoom-controls"><button class="zoom-control" id="zoomIn" type="button">+</button><button class="zoom-control" id="zoomReset" type="button">o</button><button class="zoom-control" id="zoomOut" type="button">-</button></div><div class="stage-scale" style="transform:scale(${state.zoom})"><div class="stage ${state.showHighlight ? 'highlight-on' : 'highlight-off'}" id="stage">${background ? `<div class="stage-bg" style="background-image:url('${esc(assetPath(background))}')"></div>` : ''}${state.scene?.grid?.show !== false ? `<div class="stage-grid" style="${gridStyle}"></div>${gridLabels(cols, rows)}` : ''}${state.scene ? deps.getAllItems().sort((a, b) => Number(a.layer || 0) - Number(b.layer || 0)).map(stageItem).join('') : blankMessage()}</div></div></section>`; }
    function templateModal() { const state = deps.getState(); return `<div class="modal-backdrop ${state.templateOpen ? 'is-open' : ''}" id="templateModal"><div class="modal"><div class="modal-head"><h2>Import From Templates</h2><button class="btn" id="closeTemplates" type="button">Close</button></div><div class="template-list">${state.templates.map(t => `<button class="template-card" data-template-file="${esc(t.file)}" type="button"><strong>${esc(t.label || t.id)}</strong><span>${esc(t.type || '')} · ${esc(t.file || '')}</span><span>${esc(t.defaultSaveFolder || '')}</span></button>`).join('') || '<p class="small">Template manifest has not loaded.</p>'}</div></div></div>`; }
    function contextMenu() { const state = deps.getState(); if (!state.context) return ''; if (state.context.type === 'zoom') return `<div class="context-menu" style="left:${state.context.x}px;top:${state.context.y}px"><button data-action="setZoomDefault">Set default zoom</button></div>`; const item = deps.findItem(state.context.kind, state.context.id); if (!item) return ''; return `<div class="context-menu" style="left:${state.context.x}px;top:${state.context.y}px"><div class="context-menu-head"><strong>${esc(item.name || item.id)}</strong><span>${esc(item.type || state.context.kind)}</span></div><button data-action="zoomObject">Zoom to object</button><button data-action="props">Properties</button><button data-action="duplicate">Duplicate</button><button data-action="remove">Delete</button></div>`; }
    function objectInspector() { const item = deps.getSelectedItem(); if (!item) return ''; const layout = deps.getInspectorLayout?.() || { left: 360, top: 92, closed: false }; const closed = layout.closed ? ' is-minimized' : ''; const left = Math.max(12, Number(layout.left || 360)); const top = Math.max(80, Number(layout.top || 92)); return `<section class="object-inspector${closed}" id="objectInspector" data-settings-scope="object-inspector" style="left:${left}px;top:${top}px"><header class="object-inspector-title" id="objectInspectorHandle"><div><strong>Object Inspector</strong><span>${esc(item.name || item.id)}</span></div><button class="object-inspector-minimize" id="objectInspectorMinimize" type="button" aria-label="Minimize Object Inspector">${layout.closed ? '▣' : '—'}</button></header><div class="object-inspector-body">${card('selected', 'Object Details', selectedForm(item), 'selected')}${card('transform-v35', 'Transform', transformForm(item), 'selected')}${card('visual-v15', 'Visual Adjustments', visualAdjustmentsForm(item), 'selected')}</div></section>`; }
    function shell() { return `<div class="editor-shell">${titleBar()}<main class="main-layout">${controlPanel()}${workArea()}</main></div>${objectInspector()}${templateModal()}${contextMenu()}<input class="hidden-file" id="imageFile" type="file" accept="image/*,.svg,.webp,.gif,.png,.jpg,.jpeg">`; }

    return { shell, workArea, esc, assetPath };
  }

  window.ArtifexSceneEditorRenderer = Object.freeze({ createRenderer, esc });
})();
